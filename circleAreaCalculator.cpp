#include <iostream>
#include <valarray>

using namespace std;
int main() {
    double radius;
    cout<<"Enter radius";
    cin>>radius;
    const double pi = 3.142;
    double area = pi * pow(radius, 2);
    cout<<celsius;

    return 0;
}
